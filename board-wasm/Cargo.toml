[package]
name = "board-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
sha2 = "0.10"        # 高速暗号化ハッシュ計算
getrandom = { version = "0.2", features = ["js"] }
web-sys = { version = "0.3", features = ["console"] }